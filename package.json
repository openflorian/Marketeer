{
  "name": "@niche-engine/monorepo",
  "version": "1.0.0-elite",
  "description": "NicheEngine - Elite Affiliate CMS/Storefront System für High-Performance Publisher",
  "private": true,
  "workspaces": [
    "packages/frontend",
    "packages/cms",
    "packages/api",
    "packages/ml-engine",
    "packages/edge-workers",
    "packages/shared"
  ],
  "scripts": {
    "dev": "bun run --cwd packages/frontend dev & bun run --cwd packages/cms dev & bun run --cwd packages/api dev",
    "build": "bun run --cwd packages/frontend build && bun run --cwd packages/cms build && bun run --cwd packages/api build",
    "deploy": "bun scripts/deploy.ts",
    "db:migrate": "bun run --cwd packages/cms migrate",
    "analyze": "bun scripts/performance-audit.ts",
    "ml:train": "bun run --cwd packages/ml-engine train"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "latest",
    "typescript": "^5.3.0",
    "bun-types": "latest"
  },
  "engines": {
    "bun": ">=1.0.0",
    "node": ">=18.0.0"
  }
}
